# JavaScript Array Methods: Filter and Reduce Simplified

This repository contains simple and practical JavaScript examples using `filter()` and `reduce()` — two powerful array methods commonly used in frontend and backend development.

## 🚀 What You'll Learn

- How to use `filter()` to select elements from an array
- How to use `reduce()` to accumulate or transform array data
- Real-world use cases like:
  - Filtering users by age
  - Summing numbers
  - Grouping or counting data
  - Finding max or longest value

## Clone this repository
```
git clone https://github.com/Sumedh1102/Filter-and-Reduce.git
cd Filter-and-Reduce


